{
  "name": "@langecs/stdlib",
  "version": "0.1.0",
  "description": "Standard components, systems, and agent presets for LangECS (Inbox, retry, ReAct, human-in-the-loop)",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/langecs/langecs.git",
    "directory": "packages/stdlib"
  },
  "bugs": "https://github.com/langecs/langecs/issues",
  "homepage": "https://github.com/langecs/langecs/tree/main/packages/stdlib#readme",
  "keywords": [
    "llm",
    "agents",
    "ai",
    "agent-framework",
    "ecs",
    "entity-component-system",
    "typescript",
    "react-agent",
    "tools",
    "structured-output",
    "routing"
  ],
  "sideEffects": false,
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsdown",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@langecs/core": "workspace:*"
  },
  "publishConfig": {
    "exports": {
      ".": {
        "types": "./dist/index.d.ts",
        "default": "./dist/index.js"
      }
    }
  }
}
